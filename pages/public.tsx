import React, { ReactElement } from 'react'

interface Props {
    
}

export default function publicMain({}: Props): ReactElement {
    return (
        <div>
            Public
        </div>
    )
}
