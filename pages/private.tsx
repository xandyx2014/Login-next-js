import React, { ReactElement } from 'react'

interface Props {
    
}

export default function privateMain({}: Props): ReactElement {
    return (
        <div>
            Private main
        </div>
    )
}
