import React, { ReactElement } from 'react'

interface Props {
    
}

export default function metaDescription({}: Props): ReactElement {
    return (
        <>
            <meta name="Description" content="Test next js with authentication with jwt"></meta>
        </>
    )
}
